Chance Lively

Pseudocode Paycheck Practice:

variable total
list dotw = Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday

FOR EACH day in dotw
  User Input: "hours worked" + day
    hours = Input
    total = total + hours

Print total
-------------------------
Pseudocode All Caps Practice:

variable text

User Input: "..."
text = input

text = text.to.upper
Print text
-------------------------
Pseudocode Total Practice:

variable firstNumber
variable secondNumber

User Input: "number 1"
  error catch -invalid number-
  firstNumber = input
User Input: "number 2"
  error catch -invalid number-
  secondNumber = input

Print firstNumber + secondNumber
-------------------------
Pseudocode Unknown Total Practice:

var total
boolean continue = false

While continue == false
  User Input: "number"
    error catch -invalid number-
    total = total + input
  User Input: "yes or no"
    if yes
      continue = true
    else if no
      continue == false
    else 
      invalid response

Print Total
-------------------------
Pseudocode Maximum VS Random Practice:

var maximum
var random
bool retry

User Input: "maximum number"
  maximum = input

While retry = true
  if maximum > random
    Response: "You Win :)"
  else if maximum < random
    Response: "You Lose :("

  Response: "Retry?"
  User input: "yes or no"
    if yes
      retry = ture
    else if no
      retry = false
    else
      invalid response
-------------------------
Pseudocode Maximum VS Random Practice:

var left
var right
var random

FOR i = 1 to 10
    random = random number (1 to 100)

    if i is even 
      right = right + 1
      Response: "right"
    if i is odd
      left = left + 1
      Response: "left"

Print Right amount
Print Left amount
